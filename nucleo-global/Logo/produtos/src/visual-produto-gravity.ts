/** Reexport — implementação JSX em `.tsx` (evita 404 do Vite em imports `.ts`). */
export {
  TAMANHO_ICONE_PRODUTO_HUB,
  visualProdutoGravityHub,
  visualProdutoGravityFallback,
} from './visual-produto-gravity.tsx'
export type { VisualProdutoGravity } from './visual-produto-gravity.tsx'
