{
  "scripts": {
    "db:compose": "npx ts-node scripts/compose-tenant-schema.ts",
    "db:migrate:dev": "npm run db:compose && npx prisma migrate dev --schema=configurador/prisma/schema.prisma && npx prisma migrate dev --schema=servicos-global/tenant/prisma/schema.prisma",
    "db:migrate:deploy": "npx prisma migrate deploy --schema=configurador/prisma/schema.prisma && npx prisma migrate deploy --schema=servicos-global/tenant/prisma/schema.prisma",
    "db:seed:staging": "npx ts-node scripts/seed-staging.ts",
    "db:apply-rls": "railway run psql $TENANT_DATABASE_URL -f scripts/apply-rls.sql"
  },
  "dependencies": {
    "@clerk/localizations": "^4.2.3",
    "@prisma/client": "^5.22.0"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.2",
    "playwright": "^1.58.2",
    "prisma": "^5.22.0",
    "vitest": "^4.1.2"
  }
}
