{
  "name": "gravity-bid-cambio-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/src/index.js",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "db:compose": "node prisma/compose-schema.js",
    "db:generate": "npm run db:compose && prisma generate",
    "db:migrate": "npm run db:compose && prisma migrate dev",
    "db:migrate:deploy": "npm run db:compose && prisma migrate deploy",
    "db:seed": "tsx prisma/seed.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.19.0",
    "helmet": "^7.1.0",
    "zod": "^3.23.0",
    "@prisma/client": "^5.13.0",
    "@clerk/backend": "^1.0.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "prisma": "^5.13.0",
    "tsx": "^4.11.0",
    "vitest": "^1.6.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.0.0"
  }
}
