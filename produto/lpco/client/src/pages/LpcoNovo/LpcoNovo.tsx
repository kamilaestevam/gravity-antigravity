/**
 * LpcoNovo — Wizard de criacao de LPCO
 * Step 0: Escolha de canal (manual, planilha, pedido, smart read, duplicar)
 * Step 1: Dados gerais (orgao, modelo, pais, fundamento legal)
 * Step 2: Itens NCM (quantidades, valores, atributos dinamicos)
 * Step 3: Revisao + validacao + registrar
 */

export default function LpcoNovo() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>Novo LPCO</h1>
      <p>Wizard de criacao — implementacao pendente</p>
    </div>
  )
}
