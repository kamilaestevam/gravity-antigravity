{
  "name": "gravity-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Scripts utilitários do monorepo Gravity — gerenciados pelo Coordenador",
  "scripts": {
    "compose:tenant-schema": "tsx compose-tenant-schema.ts",
    "compose:schema": "node compose-schema.js"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "tsx": "^4.11.0",
    "@types/node": "^20.0.0"
  },
  "notes": "ATENÇÃO: scripts/ é escopo exclusivo do Coordenador. Nenhum outro agente modifica arquivos aqui."
}
