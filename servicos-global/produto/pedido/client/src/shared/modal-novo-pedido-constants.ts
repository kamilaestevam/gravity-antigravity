/**
 * SSOT visual: `@nucleo/modal-passo-passo-global` → `Z_INDEX_MODAL_PASSO_PASSO`.
 * Valor espelhado aqui (testes Vitest em `testes/` não resolvem alias `@nucleo`).
 */
export const Z_INDEX_MODAL_PASSO_PASSO = 9999

/** Margem acima do wizard para confirmação cascateada (ModalOverlay). */
export const Z_INDEX_CONFIRM_DUPLICATA_MARGEM = 51

export const Z_INDEX_CONFIRM_DUPLICATA_NOVO_PEDIDO =
  Z_INDEX_MODAL_PASSO_PASSO + Z_INDEX_CONFIRM_DUPLICATA_MARGEM
