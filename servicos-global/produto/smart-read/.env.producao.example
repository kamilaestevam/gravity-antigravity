# Copie para .env.producao (gitignored) — Railway → gravity-smart-read-producao → Connect
# Use DATABASE_PUBLIC_URL se TCP Proxy estiver habilitado (host *.proxy.rlwy.net).
DATABASE_URL=postgresql://postgres:SENHA@HOST.proxy.rlwy.net:PORTA/railway
