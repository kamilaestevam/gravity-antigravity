# @tenant/api-cockpit — Variáveis de Ambiente
DATABASE_URL=
CHAVE_INTERNA_SERVICO=
PORT=8016
NODE_ENV=development

# Gerar com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
