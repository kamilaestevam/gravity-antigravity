# @tenant/cronometro — Variáveis de Ambiente
# Porta designada: 8007 (Agente Cronômetro — Onda 3 | 2/13)

# Banco de dados do tenant (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/tenant_db

# Chave interna para chamadas entre serviços
CHAVE_INTERNA_SERVICO=

# Porta do servidor (padrão: 8007)
PORT=8007

# Ambiente de execução
NODE_ENV=development
